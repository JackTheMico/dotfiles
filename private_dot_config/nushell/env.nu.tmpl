{{- template "nu-common-env" . -}}

$env.EDITOR = "lvim"
$env.hostip = (cat /etc/resolv.conf |grep -oP '(?<=nameserver\ ).*')
$env.TERM = "tmux-256color"
$env.DISPLAY = $.env.hostip:0.0
$env.GPG_TTY = (tty)
# zoxide init nushell | save -f ~/.zoxide.nu
